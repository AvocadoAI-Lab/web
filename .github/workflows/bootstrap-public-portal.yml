name: Bootstrap Avocado.ai public portal (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived bootstrap
        run: |
          echo "The bilingual portal source is now fully materialized in the repository."
          echo "Use the Public Portal CI workflow for install, build, and preview validation."
